const express = require('express'); const { body } = require('express-validator'); const authenticate = require('../middleware/authenticate'); const authorize = require('../middleware/authorize'); const validate = require('../middleware/validate'); const controller = require('../controllers/enrollment.controller'); const router = express.Router();
router.use(authenticate, authorize('student')); router.post('/', body('courseId').isMongoId(), validate, controller.enroll); router.get('/mine', controller.myEnrollments); router.get('/:id', controller.getEnrollment); module.exports = router;
