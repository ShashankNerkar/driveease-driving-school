const express = require('express');
const router  = express.Router();

// User profile routes are served via /api/students, /api/instructors,
// and /api/auth. This router is a mount-point reserved for future
// cross-role user management endpoints.

module.exports = router;
