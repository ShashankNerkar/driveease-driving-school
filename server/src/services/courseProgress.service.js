const Lesson = require('../models/Lesson'); const LessonProgress = require('../models/LessonProgress'); const { getContentCourseId } = require('./courseContent.service');
const getCourseProgress = async (studentId, courseId) => { const contentCourseId = await getContentCourseId(courseId); const lessons = await Lesson.find({ courseId: contentCourseId }).select('_id title order').sort({ order: 1 }); const lessonIds = lessons.map((lesson) => lesson._id); const completedLessons = lessonIds.length ? (await LessonProgress.find({ studentId, lessonId: { $in: lessonIds }, completed: true }).distinct('lessonId')).length : 0; const totalLessons = lessons.length; return { lessons, totalLessons, completedLessons, completionPercentage: totalLessons ? Math.round((completedLessons / totalLessons) * 100) : 0 }; };
module.exports = { getCourseProgress };
