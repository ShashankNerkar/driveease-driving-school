import { getCourseById } from '../../api/publicApi';
import PublicDetail from '../../components/public/PublicDetail';
import CourseEnrollmentAction from '../../components/course/CourseEnrollmentAction';
const CourseDetail = () => <PublicDetail request={getCourseById} itemKey="course" backTo="/courses" backLabel="All courses" render={(course) => <article className="mx-auto max-w-3xl overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm">{(course.thumbnail || course.coverImage) && <img src={course.thumbnail || course.coverImage} alt="" className="h-64 w-full object-cover" />}<div className="p-6 sm:p-8"><span className="badge bg-primary-50 text-primary-700">{course.level || course.category}</span><h1 className="mt-3 text-3xl font-bold text-gray-900">{course.title}</h1><p className="mt-4 whitespace-pre-line leading-7 text-gray-600">{course.description}</p><dl className="mt-7 border-t border-gray-100 pt-6"><div><dt className="text-xs font-medium uppercase tracking-wide text-gray-500">Duration</dt><dd className="mt-1 font-semibold">{course.duration || (course.durationWeeks ? `${course.durationWeeks} weeks` : 'Not specified')}</dd></div></dl><CourseEnrollmentAction courseId={course._id} /></div></article>} />;
export default CourseDetail;
