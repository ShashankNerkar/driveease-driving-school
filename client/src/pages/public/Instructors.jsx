import { Link } from 'react-router-dom'; import { getInstructors } from '../../api/publicApi'; import PublicResourceList from '../../components/public/PublicResourceList';
const Instructors = () => <PublicResourceList title="Meet our instructors" description="Learn with experienced instructors who help you build confident, safe driving habits." request={getInstructors} collectionKey="instructors" emptyTitle="No instructors are available right now" emptyMessage="Please check back later." renderItem={(items) => <div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{items.map((instructor) => <article key={instructor._id} className="rounded-xl border border-gray-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-4">{instructor.profileImage ? <img src={instructor.profileImage} alt="" className="h-14 w-14 rounded-full object-cover" /> : <div className="flex h-14 w-14 items-center justify-center rounded-full bg-primary-100 text-lg font-bold text-primary-700">{instructor.name?.charAt(0)}</div>}<div><h2 className="font-semibold text-gray-900">{instructor.name}</h2><p className="text-sm text-gray-500">{instructor.experience} years’ experience</p></div></div>{instructor.bio && <p className="mt-4 line-clamp-3 text-sm text-gray-600">{instructor.bio}</p>}{instructor.specialization?.length > 0 && <div className="mt-4 flex flex-wrap gap-2">{instructor.specialization.map((item) => <span key={item} className="badge bg-gray-100 text-gray-700">{item}</span>)}</div>}<Link to={`/instructors/${instructor._id}`} className="mt-5 inline-flex text-sm font-semibold text-primary-600 hover:underline">View profile →</Link></article>)}</div>} />;
export default Instructors;
