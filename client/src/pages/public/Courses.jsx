import { Link } from 'react-router-dom';
import { getCourses } from '../../api/publicApi';
import PublicResourceList from '../../components/public/PublicResourceList';

const Courses = () => <PublicResourceList title="Driving courses" description="Choose a course that matches your experience and goals." request={getCourses} collectionKey="courses" emptyTitle="No courses are available yet" emptyMessage="Our course catalogue will appear here when courses are published." renderItem={(courses) => <div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{courses.map((course) => { const level = course.level || course.category; const duration = course.duration || (course.durationWeeks ? `${course.durationWeeks} weeks` : null); return <article key={course._id} className="overflow-hidden rounded-xl border border-gray-200 bg-white shadow-sm"><div className="flex h-40 items-center justify-center bg-primary-50">{(course.thumbnail || course.coverImage) ? <img src={course.thumbnail || course.coverImage} alt="" className="h-full w-full object-cover" /> : <span className="text-sm font-medium text-primary-700">{level}</span>}</div><div className="p-5"><div className="flex items-start justify-between gap-3"><h2 className="font-semibold text-gray-900">{course.title}</h2><span className="badge shrink-0 bg-primary-50 text-primary-700">{level}</span></div><p className="mt-2 line-clamp-3 text-sm text-gray-600">{course.description}</p>{duration && <p className="mt-4 text-sm text-gray-500">Duration: {duration}</p>}<Link className="mt-5 inline-flex text-sm font-semibold text-primary-600 hover:underline" to={`/courses/${course._id}`}>View course →</Link></div></article>; })}</div>} />;
export default Courses;
