import { getInstructorById } from '../../api/publicApi'; import PublicDetail from '../../components/public/PublicDetail';
const InstructorDetail = () => <PublicDetail request={getInstructorById} itemKey="instructor" backTo="/instructors" backLabel="All instructors" render={(instructor) => <article className="mx-auto max-w-3xl rounded-xl border border-gray-200 bg-white p-6 shadow-sm sm:p-8"><div className="flex flex-col gap-5 sm:flex-row sm:items-center">{instructor.profileImage ? <img src={instructor.profileImage} alt="" className="h-24 w-24 rounded-full object-cover" /> : <div className="flex h-24 w-24 items-center justify-center rounded-full bg-primary-100 text-3xl font-bold text-primary-700">{instructor.name?.charAt(0)}</div>}<div><h1 className="text-3xl font-bold text-gray-900">{instructor.name}</h1><p className="mt-1 text-gray-500">{instructor.experience} years of teaching experience</p></div></div>{instructor.bio && <p className="mt-7 whitespace-pre-line leading-7 text-gray-600">{instructor.bio}</p>}{instructor.specialization?.length > 0 && <section className="mt-7"><h2 className="font-semibold text-gray-900">Specializations</h2><div className="mt-3 flex flex-wrap gap-2">{instructor.specialization.map((item) => <span key={item} className="badge bg-primary-50 text-primary-700">{item}</span>)}</div></section>}</article>} />;
export default InstructorDetail;
