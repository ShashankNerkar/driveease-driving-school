import { NavLink, Outlet, useNavigate } from 'react-router-dom'; import { useAuth } from '../../context/AuthContext';
import NotificationBell from '../common/NotificationBell';
const InstructorLayout = () => { const { user, logout } = useAuth(); const navigate = useNavigate(); const links = [['Dashboard', '/instructor/dashboard'], ['Profile', '/instructor/profile'], ['Availability', '/instructor/availability'], ['Bookings', '/instructor/bookings'], ['Notifications', '/instructor/notifications']]; const leave = async () => { await logout(); navigate('/', { replace: true }); }; return <div className="min-h-screen bg-gray-50"><header className="border-b bg-white"><div className="container flex min-h-16 items-center justify-between"><NavLink to="/" className="text-xl font-bold text-gray-900">DriveEase</NavLink><div className="flex items-center gap-3"><NotificationBell to="/instructor/notifications" /><span className="text-sm text-gray-600">{user?.name}</span><button className="btn-ghost" onClick={leave}>Log out</button></div></div></header><div className="container flex flex-col gap-6 py-6 lg:flex-row"><aside className="lg:w-56"><nav className="flex gap-2 rounded-xl border bg-white p-2 lg:flex-col">{links.map(([label, to]) => <NavLink key={to} to={to} className={({ isActive }) => `rounded-lg px-4 py-2.5 text-sm ${isActive ? 'bg-primary-600 text-white' : 'text-gray-600 hover:bg-gray-100'}`}>{label}</NavLink>)}</nav></aside><main className="min-w-0 flex-1"><Outlet /></main></div></div>; };
export default InstructorLayout;
